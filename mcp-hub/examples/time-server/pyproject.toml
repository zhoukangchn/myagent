[project]
name = "mcp-time-server"
version = "1.0.0"
description = "MCP Time Server - 时间服务"
requires-python = ">=3.10"
dependencies = [
    "mcp-hub",
    "pytz",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
mcp-hub = { path = "../.." }
