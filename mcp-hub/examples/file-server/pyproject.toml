[project]
name = "mcp-file-server"
version = "1.0.0"
description = "MCP File Server - 文件操作服务"
requires-python = ">=3.10"
dependencies = [
    "mcp-hub",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
mcp-hub = { path = "../.." }
