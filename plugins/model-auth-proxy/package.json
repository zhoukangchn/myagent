{
  "name": "model-auth-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw plugin: direct internal model auth with API key and custom headers",
  "type": "module",
  "openclaw": {
    "extensions": ["index.ts"]
  },
  "engines": {
    "node": ">=20"
  }
}
