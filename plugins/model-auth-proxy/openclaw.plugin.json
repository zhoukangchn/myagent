{
  "id": "model-auth-proxy",
  "name": "Model Auth Proxy",
  "description": "Direct provider auth plugin for internal model gateways using API key and custom headers",
  "version": "0.1.0",
  "providers": ["internal-model"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "tlsInsecure": {
        "type": "boolean",
        "default": false,
        "description": "Disable TLS cert verification for upstream requests (testing only)"
      }
    }
  }
}
