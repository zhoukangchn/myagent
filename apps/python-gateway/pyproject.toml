[project]
name = "python-chat-sse-gateway"
version = "0.1.0"
description = "Python FastAPI gateway for SSE chat ingress and OpenClaw WebSocket bridge"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "pydantic>=2.6.0",
  "pydantic-settings>=2.4.0",
  "uvicorn>=0.30.0",
  "websockets>=14.0",
]

[project.optional-dependencies]
dev = [
  "httpx>=0.27.0",
  "pytest>=8.0.0",
  "pytest-asyncio>=0.23.0",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["gateway*"]

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"
